const https = require('https');
function search(q) { const url = 'https://www.youtube.com/results?search_query=' + encodeURIComponent(q) + '&sp=EgIQAw%3D%3D'; https.get(url, { headers: { 'User-Agent': 'Mozilla/5.0' } }, (res) => { let data = ''; res.on('data', c => data += c); res.on('end', () => { const m = data.match(/var ytInitialData = ({.+?});/); if(m) { const d = JSON.parse(m[1]); const c = d.contents?.twoColumnSearchResultsRenderer?.primaryContents?.sectionListRenderer?.contents; if(c) { try { const r = c[0].itemSectionRenderer.contents[0].playlistRenderer; console.log(q, r.playlistId, r.title.simpleText || r.title.runs[0].text); } catch(e) {} } } }); }); }
search('ลูกทุ่ง 100 ล้านวิว'); search('GMM Grammy ฮิต'); search('T-Pop Hits'); search('เพลงฮิตยุค 2000'); search('เพลงใหม่ล่าสุด');
