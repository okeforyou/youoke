# การตั้งค่า Google OAuth สำหรับ Firebase

## ขั้นตอนการเปิดใช้งาน Google Sign-In

### 1. เปิด Firebase Console
1. ไปที่ https://console.firebase.google.com/
2. เลือกโปรเจกต์ของคุณ

### 2. เปิดใช้งาน Google Authentication
1. ไปที่เมนู **Authentication** (ด้านซ้าย)
2. คลิกแท็บ **Sign-in method**
3. ค้นหา **Google** ในรายการ Providers
4. คลิกปุ่ม **Edit** (ไอคอนดินสอ)
5. **เปิดใช้งาน** (Enable) โดยเลื่อนสวิตช์
6. กรอก **Project support email** (เลือกอีเมลของคุณ)
7. คลิก **Save**

### 3. เพิ่ม Authorized Domains
Firebase จะเพิ่ม localhost และโดเมนอัตโนมัติ แต่ถ้าคุณมีโดเมนเพิ่มเติม:

1. ในหน้า **Sign-in method**
2. เลื่อนลงไปที่ **Authorized domains**
3. คลิก **Add domain**
4. เพิ่มโดเมนของคุณ:
   - `play.okeforyou.com`
   - `clone.okeforyou.com`
   - หรือโดเมนอื่นๆ ที่ใช้งาน

### 4. ทดสอบการทำงาน

#### หน้า Login
1. ไปที่ `/login`
2. จะเห็นปุ่ม **"เข้าสู่ระบบด้วย Google"** ด้านล่างปุ่ม "สมัครสมาชิก"
3. คลิกปุ่มจะเปิด popup ให้เลือกบัญชี Google
4. เลือกบัญชีที่ต้องการ
5. ระบบจะ redirect ไปหน้าหลักอัตโนมัติ

#### หน้า Register
1. ไปที่ `/register`
2. เลือกแพ็กเกจที่ต้องการ
3. จะเห็นปุ่ม **"สมัครด้วย Google"** เหนือปุ่ม "สมัครสมาชิกด้วยอีเมล"
4. คลิกปุ่มจะเปิด popup ให้เลือกบัญชี Google
5. ระบบจะสร้าง user profile อัตโนมัติจากข้อมูล Google:
   - Email จาก Google Account
   - ชื่อ-นามสกุล จาก Google Display Name
   - Phone จะเป็นค่าว่าง (ให้ผู้ใช้เพิ่มเองภายหลัง)
6. Redirect ไปหน้าหลัก (FREE) หรือหน้า payment (Paid plans)

### 5. การจัดการ User ที่ Sign In ด้วย Google

ใน Firebase Console:
1. ไปที่ **Authentication** > **Users**
2. จะเห็นผู้ใช้ที่ sign in ด้วย Google มี Provider เป็น **google.com**
3. ข้อมูลจะรวม:
   - UID (Firebase User ID)
   - Email (จาก Google)
   - Display Name (จาก Google)
   - Photo URL (จาก Google Profile Picture)
   - Sign-in method: Google

### 6. ข้อดีของ Google Sign-In

✅ **สำหรับผู้ใช้:**
- ลงทะเบียนง่ายขึ้น (ไม่ต้องจำรหัสผ่าน)
- เข้าสู่ระบบเร็วขึ้น (1 คลิก)
- ปลอดภัยกว่า (Google จัดการ authentication)
- Auto-fill ข้อมูลจาก Google Account

✅ **สำหรับเว็บไซต์:**
- ลด friction ในการลงทะเบียน
- เพิ่ม conversion rate
- ลดปัญหา "ลืมรหัสผ่าน"
- ได้ข้อมูลผู้ใช้ที่ verified แล้วจาก Google

### 7. Troubleshooting

**ปัญหา: Popup ไม่เปิด**
- ตรวจสอบว่า browser ไม่ได้บล็อก popup
- ลองใช้ Incognito/Private mode

**ปัญหา: "Unauthorized domain"**
- ตรวจสอบว่าโดเมนที่ใช้งานอยู่ใน Authorized domains

**ปัญหา: "API not enabled"**
- ตรวจสอบว่าเปิดใช้งาน Google Sign-In ใน Firebase Console แล้ว

**ปัญหา: User profile ไม่ถูกสร้าง**
- ตรวจสอบ Console log สำหรับ error
- ตรวจสอบว่า Firestore rules อนุญาตให้เขียนข้อมูลได้

## โค้ดที่เกี่ยวข้อง

### Files ที่ถูกแก้ไข:
- `context/AuthContext.tsx` - เพิ่ม `signInWithGoogle()` method
- `components/LoginForm.tsx` - เพิ่มปุ่ม Google Sign-In
- `pages/register.tsx` - เพิ่มปุ่ม Google Sign-Up

### Firebase Configuration:
ไม่จำเป็นต้องเพิ่มอะไรใน `.env` เพราะ Firebase SDK จะใช้ config ที่มีอยู่แล้วใน `firebase.ts`

## เสร็จสิ้น!

ตอนนี้ผู้ใช้สามารถ Sign in ด้วย Google ได้แล้ว 🎉
